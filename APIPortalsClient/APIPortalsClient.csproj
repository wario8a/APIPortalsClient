<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <GraphQL Remove="CaseById.graphql" />
    <GraphQL Remove="GetCases.graphql" />
    <GraphQL Remove="GetPeople.graphql" />
    <GraphQL Remove="QueryAccounts.graphql" />
    <GraphQL Remove="UpdateAccount.graphql" />
    <GraphQL Remove="UpdatePerson.graphql" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="StrawberryShake.Server" Version="13.9.12" />
  </ItemGroup>

</Project>
